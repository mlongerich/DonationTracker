# Production Environment Variables for Docker Deployment
# Used by docker-compose.prod.yml
#
# IMPORTANT: Copy this to .env and fill in actual values
#   cp .env.production.example .env
#   nano .env
#
# Security: DO NOT commit .env file to git
#   (Already in .gitignore)

# ============================================================================
# Database Configuration
# ============================================================================
DB_USER=donation_tracker
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD_16_CHARS
DB_NAME=donation_tracker_production

# ============================================================================
# Rails Security Secrets
# ============================================================================
# Generate with: docker-compose run --rm api rails secret
SECRET_KEY_BASE=CHANGE_ME_GENERATE_WITH_RAILS_SECRET
JWT_SECRET_KEY=CHANGE_ME_GENERATE_WITH_RAILS_SECRET

# ============================================================================
# CORS & Frontend Configuration
# ============================================================================
CORS_ALLOWED_ORIGINS=https://donations.projectsforasia.com
FRONTEND_URL=https://donations.projectsforasia.com

# ============================================================================
# Google OAuth2 (TICKET-136)
# ============================================================================
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=CHANGE_ME_YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=CHANGE_ME_YOUR_GOOGLE_CLIENT_SECRET

# ============================================================================
# Performance Tuning (512MB RAM)
# ============================================================================
# These are set in docker-compose.prod.yml, but can be overridden here
# WEB_CONCURRENCY=0       # Single mode (no workers)
# RAILS_MAX_THREADS=2     # Reduced from 5
# PORT=3001

# ============================================================================
# SECURITY CHECKLIST
# ============================================================================
# Before deploying, verify:
# ✓ All CHANGE_ME values replaced with actual secrets
# ✓ DB_PASSWORD is strong (16+ characters, mixed case, numbers, symbols)
# ✓ SECRET_KEY_BASE generated with 'rails secret' (128 chars)
# ✓ JWT_SECRET_KEY generated with 'rails secret' (128 chars)
# ✓ CORS_ALLOWED_ORIGINS matches your production domain exactly
# ✓ Google OAuth credentials configured for production domain
# ✓ File named .env (not .env.production or .env.example)
# ✓ File permissions: chmod 600 .env
# ✓ This .env file is NOT committed to git
